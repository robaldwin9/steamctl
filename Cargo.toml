[package]
name = "steamctl"
version = "0.0.1"
edition = "2024"
license = "MIT"

[dependencies]
fuzzy-matcher = "0.3.7"
glob = "0.3"
clap = { version = "4", features = ["derive"] }
nix = { version = "0.29", features = ["signal"] }
ureq = "2"
serde_json = "1"
